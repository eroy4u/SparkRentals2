CREATE TABLE rentals ( id text PRIMARY KEY, city text, province text,
    country text, zipCode text, type text, hasAirCondition boolean,
    hasGarden boolean, hasPool boolean, isCloseToBeach boolean, dailyPrice float,
    currency text, roomsNumber int, updated timestamp);


